import { DataTablesDirectiveDirective } from './data-tables-directive.directive';

describe('DataTablesDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DataTablesDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
